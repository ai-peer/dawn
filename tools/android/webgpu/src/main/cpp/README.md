1. Create a symlink in this folder named 'dawn', to your local 'dawn' folder.
../../../../../../../dawn
```
ln -s dawn /path/to/my/dawn
```
2. Go back to the android folder and Build with `./gradlew publishToMavenLocal`
