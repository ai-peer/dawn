#include <metal_stdlib>

using namespace metal;
kernel void f() {
  volatile bool tint_loop_preserving_true = true;
  for(int i = 0; ((i < 4)) && tint_loop_preserving_true; i = as_type<int>((as_type<uint>(i) + as_type<uint>(1)))) {
    switch(i) {
      case 0: {
        continue;
        break;
      }
      default: {
        break;
      }
    }
  }
  return;
}

