#include <metal_stdlib>

using namespace metal;
kernel void tint_symbol() {
  volatile bool tint_loop_preserving_true = true;
  int i = 0;
  for(int i_1 = 0; ((i_1 < 2)) && tint_loop_preserving_true; i_1 = as_type<int>((as_type<uint>(i_1) + as_type<uint>(1)))) {
    switch(i_1) {
      case 0: {
        continue;
        break;
      }
      default: {
        break;
      }
    }
    switch(i_1) {
      case 0: {
        continue;
        break;
      }
      default: {
        break;
      }
    }
  }
  return;
}

