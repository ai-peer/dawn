#include <metal_stdlib>

using namespace metal;

constant static volatile bool tint_volatile_true = true;
#define TINT_ISOLATE_UB if(tint_volatile_true) 

int f() {
  int i = 0;
  TINT_ISOLATE_UB while(true) {
    if ((i > 4)) {
      return i;
    }
    {
      i = as_type<int>((as_type<uint>(i) + as_type<uint>(1)));
      if ((i == 4)) { break; }
    }
  }
  return i;
}

