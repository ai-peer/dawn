SKIP: FAILED

#include <metal_stdlib>
using namespace metal;

kernel void f(uint3 v) {
  uint const l = (v[0u] << (v[1u] % 1u));
}
program_source:4:15: error: invalid type 'uint3' (vector of 3 'unsigned int' values) for input declaration in a kernel function
kernel void f(uint3 v) {
              ^~~~~~~

