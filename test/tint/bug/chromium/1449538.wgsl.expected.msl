#include <metal_stdlib>

using namespace metal;
void f() {
  volatile bool tint_loop_preserving_true = true;
  for(int i0520 = 0; (false) && tint_loop_preserving_true; ) {
  }
  for(int i62 = 0; (false) && tint_loop_preserving_true; ) {
  }
  for(int i0520 = 0; (false) && tint_loop_preserving_true; ) {
  }
  for(int i62 = 0; (false) && tint_loop_preserving_true; ) {
  }
  for(int i62 = 0; (false) && tint_loop_preserving_true; ) {
  }
  for(int i60 = 0; (false) && tint_loop_preserving_true; ) {
  }
  for(int i62 = 0; (false) && tint_loop_preserving_true; ) {
  }
  for(int i60 = 0; (false) && tint_loop_preserving_true; ) {
  }
}

