#include <metal_stdlib>

using namespace metal;
bool func_3(const constant int* const tint_symbol_1) {
  volatile bool tint_loop_preserving_true = true;
  for(int i = 0; ((i < *(tint_symbol_1))) && tint_loop_preserving_true; i = as_type<int>((as_type<uint>(i) + as_type<uint>(1)))) {
    for(int j = -1; ((j == 1)) && tint_loop_preserving_true; j = as_type<int>((as_type<uint>(j) + as_type<uint>(1)))) {
      return false;
    }
  }
  return false;
}

kernel void tint_symbol(const constant int* tint_symbol_2 [[buffer(0)]]) {
  volatile bool tint_loop_preserving_true_1 = true;
  func_3(tint_symbol_2);
  return;
}

