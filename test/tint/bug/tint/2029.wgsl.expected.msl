#include <metal_stdlib>

using namespace metal;
kernel void tint_symbol(device packed_int3* tint_symbol_1 [[buffer(0)]]) {
  volatile bool tint_loop_preserving_true = true;
  *(tint_symbol_1) = packed_int3(1);
  return;
}

