#include <metal_stdlib>

using namespace metal;
struct tint_packed_vec3_f16_struct_member {
  /* 0x0000 */ packed_half3 elements;
};

kernel void tint_symbol(const constant tint_packed_vec3_f16_struct_member* tint_symbol_1 [[buffer(0)]]) {
  half3 const x = half3((*(tint_symbol_1)).elements);
  return;
}

