#include <metal_stdlib>

using namespace metal;
struct tint_packed_vec3_i32_struct_member {
  /* 0x0000 */ packed_int3 elements;
};

kernel void tint_symbol(const constant tint_packed_vec3_i32_struct_member* tint_symbol_1 [[buffer(0)]]) {
  int3 const x = int3((*(tint_symbol_1)).elements);
  return;
}

