#include <metal_stdlib>

using namespace metal;
kernel void tint_symbol(const constant float* tint_symbol_1 [[buffer(0)]], device float* tint_symbol_2 [[buffer(1)]]) {
  volatile bool tint_loop_preserving_true = true;
  float const x = *(tint_symbol_1);
  *(tint_symbol_2) = x;
  return;
}

