#include <metal_stdlib>

using namespace metal;
kernel void f() {
  volatile bool tint_loop_preserving_true = true;
  float const a = 1.0f;
  float const b = 2.0f;
  float const r = (a - b);
  return;
}

