#include <metal_stdlib>

using namespace metal;

template<typename T, size_t N>
struct tint_array {
    const constant T& operator[](size_t i) const constant { return elements[i]; }
    device T& operator[](size_t i) device { return elements[i]; }
    const device T& operator[](size_t i) const device { return elements[i]; }
    thread T& operator[](size_t i) thread { return elements[i]; }
    const thread T& operator[](size_t i) const thread { return elements[i]; }
    threadgroup T& operator[](size_t i) threadgroup { return elements[i]; }
    const threadgroup T& operator[](size_t i) const threadgroup { return elements[i]; }
    T elements[N];
};

struct tint_packed_vec3_i32_struct_member {
  /* 0x0000 */ packed_int3 elements;
};

struct S_tint_packed_vec3 {
  /* 0x0000 */ tint_packed_vec3_i32_struct_member v;
  /* 0x000c */ tint_array<int8_t, 4> tint_pad;
};

struct S {
  int3 v;
};

void f(const constant S_tint_packed_vec3* const tint_symbol) {
  int3 v = int3((*(tint_symbol)).v.elements);
  int x = (*(tint_symbol)).v.elements[0];
  int y = (*(tint_symbol)).v.elements[1];
  int z = (*(tint_symbol)).v.elements[2];
  int2 xx = int3((*(tint_symbol)).v.elements).xx;
  int2 xy = int3((*(tint_symbol)).v.elements).xy;
  int2 xz = int3((*(tint_symbol)).v.elements).xz;
  int2 yx = int3((*(tint_symbol)).v.elements).yx;
  int2 yy = int3((*(tint_symbol)).v.elements).yy;
  int2 yz = int3((*(tint_symbol)).v.elements).yz;
  int2 zx = int3((*(tint_symbol)).v.elements).zx;
  int2 zy = int3((*(tint_symbol)).v.elements).zy;
  int2 zz = int3((*(tint_symbol)).v.elements).zz;
  int3 xxx = int3((*(tint_symbol)).v.elements).xxx;
  int3 xxy = int3((*(tint_symbol)).v.elements).xxy;
  int3 xxz = int3((*(tint_symbol)).v.elements).xxz;
  int3 xyx = int3((*(tint_symbol)).v.elements).xyx;
  int3 xyy = int3((*(tint_symbol)).v.elements).xyy;
  int3 xyz = int3((*(tint_symbol)).v.elements).xyz;
  int3 xzx = int3((*(tint_symbol)).v.elements).xzx;
  int3 xzy = int3((*(tint_symbol)).v.elements).xzy;
  int3 xzz = int3((*(tint_symbol)).v.elements).xzz;
  int3 yxx = int3((*(tint_symbol)).v.elements).yxx;
  int3 yxy = int3((*(tint_symbol)).v.elements).yxy;
  int3 yxz = int3((*(tint_symbol)).v.elements).yxz;
  int3 yyx = int3((*(tint_symbol)).v.elements).yyx;
  int3 yyy = int3((*(tint_symbol)).v.elements).yyy;
  int3 yyz = int3((*(tint_symbol)).v.elements).yyz;
  int3 yzx = int3((*(tint_symbol)).v.elements).yzx;
  int3 yzy = int3((*(tint_symbol)).v.elements).yzy;
  int3 yzz = int3((*(tint_symbol)).v.elements).yzz;
  int3 zxx = int3((*(tint_symbol)).v.elements).zxx;
  int3 zxy = int3((*(tint_symbol)).v.elements).zxy;
  int3 zxz = int3((*(tint_symbol)).v.elements).zxz;
  int3 zyx = int3((*(tint_symbol)).v.elements).zyx;
  int3 zyy = int3((*(tint_symbol)).v.elements).zyy;
  int3 zyz = int3((*(tint_symbol)).v.elements).zyz;
  int3 zzx = int3((*(tint_symbol)).v.elements).zzx;
  int3 zzy = int3((*(tint_symbol)).v.elements).zzy;
  int3 zzz = int3((*(tint_symbol)).v.elements).zzz;
  int4 xxxx = int3((*(tint_symbol)).v.elements).xxxx;
  int4 xxxy = int3((*(tint_symbol)).v.elements).xxxy;
  int4 xxxz = int3((*(tint_symbol)).v.elements).xxxz;
  int4 xxyx = int3((*(tint_symbol)).v.elements).xxyx;
  int4 xxyy = int3((*(tint_symbol)).v.elements).xxyy;
  int4 xxyz = int3((*(tint_symbol)).v.elements).xxyz;
  int4 xxzx = int3((*(tint_symbol)).v.elements).xxzx;
  int4 xxzy = int3((*(tint_symbol)).v.elements).xxzy;
  int4 xxzz = int3((*(tint_symbol)).v.elements).xxzz;
  int4 xyxx = int3((*(tint_symbol)).v.elements).xyxx;
  int4 xyxy = int3((*(tint_symbol)).v.elements).xyxy;
  int4 xyxz = int3((*(tint_symbol)).v.elements).xyxz;
  int4 xyyx = int3((*(tint_symbol)).v.elements).xyyx;
  int4 xyyy = int3((*(tint_symbol)).v.elements).xyyy;
  int4 xyyz = int3((*(tint_symbol)).v.elements).xyyz;
  int4 xyzx = int3((*(tint_symbol)).v.elements).xyzx;
  int4 xyzy = int3((*(tint_symbol)).v.elements).xyzy;
  int4 xyzz = int3((*(tint_symbol)).v.elements).xyzz;
  int4 xzxx = int3((*(tint_symbol)).v.elements).xzxx;
  int4 xzxy = int3((*(tint_symbol)).v.elements).xzxy;
  int4 xzxz = int3((*(tint_symbol)).v.elements).xzxz;
  int4 xzyx = int3((*(tint_symbol)).v.elements).xzyx;
  int4 xzyy = int3((*(tint_symbol)).v.elements).xzyy;
  int4 xzyz = int3((*(tint_symbol)).v.elements).xzyz;
  int4 xzzx = int3((*(tint_symbol)).v.elements).xzzx;
  int4 xzzy = int3((*(tint_symbol)).v.elements).xzzy;
  int4 xzzz = int3((*(tint_symbol)).v.elements).xzzz;
  int4 yxxx = int3((*(tint_symbol)).v.elements).yxxx;
  int4 yxxy = int3((*(tint_symbol)).v.elements).yxxy;
  int4 yxxz = int3((*(tint_symbol)).v.elements).yxxz;
  int4 yxyx = int3((*(tint_symbol)).v.elements).yxyx;
  int4 yxyy = int3((*(tint_symbol)).v.elements).yxyy;
  int4 yxyz = int3((*(tint_symbol)).v.elements).yxyz;
  int4 yxzx = int3((*(tint_symbol)).v.elements).yxzx;
  int4 yxzy = int3((*(tint_symbol)).v.elements).yxzy;
  int4 yxzz = int3((*(tint_symbol)).v.elements).yxzz;
  int4 yyxx = int3((*(tint_symbol)).v.elements).yyxx;
  int4 yyxy = int3((*(tint_symbol)).v.elements).yyxy;
  int4 yyxz = int3((*(tint_symbol)).v.elements).yyxz;
  int4 yyyx = int3((*(tint_symbol)).v.elements).yyyx;
  int4 yyyy = int3((*(tint_symbol)).v.elements).yyyy;
  int4 yyyz = int3((*(tint_symbol)).v.elements).yyyz;
  int4 yyzx = int3((*(tint_symbol)).v.elements).yyzx;
  int4 yyzy = int3((*(tint_symbol)).v.elements).yyzy;
  int4 yyzz = int3((*(tint_symbol)).v.elements).yyzz;
  int4 yzxx = int3((*(tint_symbol)).v.elements).yzxx;
  int4 yzxy = int3((*(tint_symbol)).v.elements).yzxy;
  int4 yzxz = int3((*(tint_symbol)).v.elements).yzxz;
  int4 yzyx = int3((*(tint_symbol)).v.elements).yzyx;
  int4 yzyy = int3((*(tint_symbol)).v.elements).yzyy;
  int4 yzyz = int3((*(tint_symbol)).v.elements).yzyz;
  int4 yzzx = int3((*(tint_symbol)).v.elements).yzzx;
  int4 yzzy = int3((*(tint_symbol)).v.elements).yzzy;
  int4 yzzz = int3((*(tint_symbol)).v.elements).yzzz;
  int4 zxxx = int3((*(tint_symbol)).v.elements).zxxx;
  int4 zxxy = int3((*(tint_symbol)).v.elements).zxxy;
  int4 zxxz = int3((*(tint_symbol)).v.elements).zxxz;
  int4 zxyx = int3((*(tint_symbol)).v.elements).zxyx;
  int4 zxyy = int3((*(tint_symbol)).v.elements).zxyy;
  int4 zxyz = int3((*(tint_symbol)).v.elements).zxyz;
  int4 zxzx = int3((*(tint_symbol)).v.elements).zxzx;
  int4 zxzy = int3((*(tint_symbol)).v.elements).zxzy;
  int4 zxzz = int3((*(tint_symbol)).v.elements).zxzz;
  int4 zyxx = int3((*(tint_symbol)).v.elements).zyxx;
  int4 zyxy = int3((*(tint_symbol)).v.elements).zyxy;
  int4 zyxz = int3((*(tint_symbol)).v.elements).zyxz;
  int4 zyyx = int3((*(tint_symbol)).v.elements).zyyx;
  int4 zyyy = int3((*(tint_symbol)).v.elements).zyyy;
  int4 zyyz = int3((*(tint_symbol)).v.elements).zyyz;
  int4 zyzx = int3((*(tint_symbol)).v.elements).zyzx;
  int4 zyzy = int3((*(tint_symbol)).v.elements).zyzy;
  int4 zyzz = int3((*(tint_symbol)).v.elements).zyzz;
  int4 zzxx = int3((*(tint_symbol)).v.elements).zzxx;
  int4 zzxy = int3((*(tint_symbol)).v.elements).zzxy;
  int4 zzxz = int3((*(tint_symbol)).v.elements).zzxz;
  int4 zzyx = int3((*(tint_symbol)).v.elements).zzyx;
  int4 zzyy = int3((*(tint_symbol)).v.elements).zzyy;
  int4 zzyz = int3((*(tint_symbol)).v.elements).zzyz;
  int4 zzzx = int3((*(tint_symbol)).v.elements).zzzx;
  int4 zzzy = int3((*(tint_symbol)).v.elements).zzzy;
  int4 zzzz = int3((*(tint_symbol)).v.elements).zzzz;
}

