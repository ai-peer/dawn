#include <metal_stdlib>

using namespace metal;

template<typename T, size_t N>
struct tint_array {
    const constant T& operator[](size_t i) const constant { return elements[i]; }
    device T& operator[](size_t i) device { return elements[i]; }
    const device T& operator[](size_t i) const device { return elements[i]; }
    thread T& operator[](size_t i) thread { return elements[i]; }
    const thread T& operator[](size_t i) const thread { return elements[i]; }
    threadgroup T& operator[](size_t i) threadgroup { return elements[i]; }
    const threadgroup T& operator[](size_t i) const threadgroup { return elements[i]; }
    T elements[N];
};

struct tint_packed_vec3_f16_struct_member {
  /* 0x0000 */ packed_half3 elements;
};

struct S_tint_packed_vec3 {
  /* 0x0000 */ tint_packed_vec3_f16_struct_member v;
  /* 0x0006 */ tint_array<int8_t, 2> tint_pad;
};

struct S {
  half3 v;
};

void f(const constant S_tint_packed_vec3* const tint_symbol) {
  half3 v = half3((*(tint_symbol)).v.elements);
  half x = (*(tint_symbol)).v.elements[0];
  half y = (*(tint_symbol)).v.elements[1];
  half z = (*(tint_symbol)).v.elements[2];
  half2 xx = half3((*(tint_symbol)).v.elements).xx;
  half2 xy = half3((*(tint_symbol)).v.elements).xy;
  half2 xz = half3((*(tint_symbol)).v.elements).xz;
  half2 yx = half3((*(tint_symbol)).v.elements).yx;
  half2 yy = half3((*(tint_symbol)).v.elements).yy;
  half2 yz = half3((*(tint_symbol)).v.elements).yz;
  half2 zx = half3((*(tint_symbol)).v.elements).zx;
  half2 zy = half3((*(tint_symbol)).v.elements).zy;
  half2 zz = half3((*(tint_symbol)).v.elements).zz;
  half3 xxx = half3((*(tint_symbol)).v.elements).xxx;
  half3 xxy = half3((*(tint_symbol)).v.elements).xxy;
  half3 xxz = half3((*(tint_symbol)).v.elements).xxz;
  half3 xyx = half3((*(tint_symbol)).v.elements).xyx;
  half3 xyy = half3((*(tint_symbol)).v.elements).xyy;
  half3 xyz = half3((*(tint_symbol)).v.elements).xyz;
  half3 xzx = half3((*(tint_symbol)).v.elements).xzx;
  half3 xzy = half3((*(tint_symbol)).v.elements).xzy;
  half3 xzz = half3((*(tint_symbol)).v.elements).xzz;
  half3 yxx = half3((*(tint_symbol)).v.elements).yxx;
  half3 yxy = half3((*(tint_symbol)).v.elements).yxy;
  half3 yxz = half3((*(tint_symbol)).v.elements).yxz;
  half3 yyx = half3((*(tint_symbol)).v.elements).yyx;
  half3 yyy = half3((*(tint_symbol)).v.elements).yyy;
  half3 yyz = half3((*(tint_symbol)).v.elements).yyz;
  half3 yzx = half3((*(tint_symbol)).v.elements).yzx;
  half3 yzy = half3((*(tint_symbol)).v.elements).yzy;
  half3 yzz = half3((*(tint_symbol)).v.elements).yzz;
  half3 zxx = half3((*(tint_symbol)).v.elements).zxx;
  half3 zxy = half3((*(tint_symbol)).v.elements).zxy;
  half3 zxz = half3((*(tint_symbol)).v.elements).zxz;
  half3 zyx = half3((*(tint_symbol)).v.elements).zyx;
  half3 zyy = half3((*(tint_symbol)).v.elements).zyy;
  half3 zyz = half3((*(tint_symbol)).v.elements).zyz;
  half3 zzx = half3((*(tint_symbol)).v.elements).zzx;
  half3 zzy = half3((*(tint_symbol)).v.elements).zzy;
  half3 zzz = half3((*(tint_symbol)).v.elements).zzz;
  half4 xxxx = half3((*(tint_symbol)).v.elements).xxxx;
  half4 xxxy = half3((*(tint_symbol)).v.elements).xxxy;
  half4 xxxz = half3((*(tint_symbol)).v.elements).xxxz;
  half4 xxyx = half3((*(tint_symbol)).v.elements).xxyx;
  half4 xxyy = half3((*(tint_symbol)).v.elements).xxyy;
  half4 xxyz = half3((*(tint_symbol)).v.elements).xxyz;
  half4 xxzx = half3((*(tint_symbol)).v.elements).xxzx;
  half4 xxzy = half3((*(tint_symbol)).v.elements).xxzy;
  half4 xxzz = half3((*(tint_symbol)).v.elements).xxzz;
  half4 xyxx = half3((*(tint_symbol)).v.elements).xyxx;
  half4 xyxy = half3((*(tint_symbol)).v.elements).xyxy;
  half4 xyxz = half3((*(tint_symbol)).v.elements).xyxz;
  half4 xyyx = half3((*(tint_symbol)).v.elements).xyyx;
  half4 xyyy = half3((*(tint_symbol)).v.elements).xyyy;
  half4 xyyz = half3((*(tint_symbol)).v.elements).xyyz;
  half4 xyzx = half3((*(tint_symbol)).v.elements).xyzx;
  half4 xyzy = half3((*(tint_symbol)).v.elements).xyzy;
  half4 xyzz = half3((*(tint_symbol)).v.elements).xyzz;
  half4 xzxx = half3((*(tint_symbol)).v.elements).xzxx;
  half4 xzxy = half3((*(tint_symbol)).v.elements).xzxy;
  half4 xzxz = half3((*(tint_symbol)).v.elements).xzxz;
  half4 xzyx = half3((*(tint_symbol)).v.elements).xzyx;
  half4 xzyy = half3((*(tint_symbol)).v.elements).xzyy;
  half4 xzyz = half3((*(tint_symbol)).v.elements).xzyz;
  half4 xzzx = half3((*(tint_symbol)).v.elements).xzzx;
  half4 xzzy = half3((*(tint_symbol)).v.elements).xzzy;
  half4 xzzz = half3((*(tint_symbol)).v.elements).xzzz;
  half4 yxxx = half3((*(tint_symbol)).v.elements).yxxx;
  half4 yxxy = half3((*(tint_symbol)).v.elements).yxxy;
  half4 yxxz = half3((*(tint_symbol)).v.elements).yxxz;
  half4 yxyx = half3((*(tint_symbol)).v.elements).yxyx;
  half4 yxyy = half3((*(tint_symbol)).v.elements).yxyy;
  half4 yxyz = half3((*(tint_symbol)).v.elements).yxyz;
  half4 yxzx = half3((*(tint_symbol)).v.elements).yxzx;
  half4 yxzy = half3((*(tint_symbol)).v.elements).yxzy;
  half4 yxzz = half3((*(tint_symbol)).v.elements).yxzz;
  half4 yyxx = half3((*(tint_symbol)).v.elements).yyxx;
  half4 yyxy = half3((*(tint_symbol)).v.elements).yyxy;
  half4 yyxz = half3((*(tint_symbol)).v.elements).yyxz;
  half4 yyyx = half3((*(tint_symbol)).v.elements).yyyx;
  half4 yyyy = half3((*(tint_symbol)).v.elements).yyyy;
  half4 yyyz = half3((*(tint_symbol)).v.elements).yyyz;
  half4 yyzx = half3((*(tint_symbol)).v.elements).yyzx;
  half4 yyzy = half3((*(tint_symbol)).v.elements).yyzy;
  half4 yyzz = half3((*(tint_symbol)).v.elements).yyzz;
  half4 yzxx = half3((*(tint_symbol)).v.elements).yzxx;
  half4 yzxy = half3((*(tint_symbol)).v.elements).yzxy;
  half4 yzxz = half3((*(tint_symbol)).v.elements).yzxz;
  half4 yzyx = half3((*(tint_symbol)).v.elements).yzyx;
  half4 yzyy = half3((*(tint_symbol)).v.elements).yzyy;
  half4 yzyz = half3((*(tint_symbol)).v.elements).yzyz;
  half4 yzzx = half3((*(tint_symbol)).v.elements).yzzx;
  half4 yzzy = half3((*(tint_symbol)).v.elements).yzzy;
  half4 yzzz = half3((*(tint_symbol)).v.elements).yzzz;
  half4 zxxx = half3((*(tint_symbol)).v.elements).zxxx;
  half4 zxxy = half3((*(tint_symbol)).v.elements).zxxy;
  half4 zxxz = half3((*(tint_symbol)).v.elements).zxxz;
  half4 zxyx = half3((*(tint_symbol)).v.elements).zxyx;
  half4 zxyy = half3((*(tint_symbol)).v.elements).zxyy;
  half4 zxyz = half3((*(tint_symbol)).v.elements).zxyz;
  half4 zxzx = half3((*(tint_symbol)).v.elements).zxzx;
  half4 zxzy = half3((*(tint_symbol)).v.elements).zxzy;
  half4 zxzz = half3((*(tint_symbol)).v.elements).zxzz;
  half4 zyxx = half3((*(tint_symbol)).v.elements).zyxx;
  half4 zyxy = half3((*(tint_symbol)).v.elements).zyxy;
  half4 zyxz = half3((*(tint_symbol)).v.elements).zyxz;
  half4 zyyx = half3((*(tint_symbol)).v.elements).zyyx;
  half4 zyyy = half3((*(tint_symbol)).v.elements).zyyy;
  half4 zyyz = half3((*(tint_symbol)).v.elements).zyyz;
  half4 zyzx = half3((*(tint_symbol)).v.elements).zyzx;
  half4 zyzy = half3((*(tint_symbol)).v.elements).zyzy;
  half4 zyzz = half3((*(tint_symbol)).v.elements).zyzz;
  half4 zzxx = half3((*(tint_symbol)).v.elements).zzxx;
  half4 zzxy = half3((*(tint_symbol)).v.elements).zzxy;
  half4 zzxz = half3((*(tint_symbol)).v.elements).zzxz;
  half4 zzyx = half3((*(tint_symbol)).v.elements).zzyx;
  half4 zzyy = half3((*(tint_symbol)).v.elements).zzyy;
  half4 zzyz = half3((*(tint_symbol)).v.elements).zzyz;
  half4 zzzx = half3((*(tint_symbol)).v.elements).zzzx;
  half4 zzzy = half3((*(tint_symbol)).v.elements).zzzy;
  half4 zzzz = half3((*(tint_symbol)).v.elements).zzzz;
}

