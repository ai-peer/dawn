#include <metal_stdlib>

using namespace metal;

template<typename T, size_t N>
struct tint_array {
    const constant T& operator[](size_t i) const constant { return elements[i]; }
    device T& operator[](size_t i) device { return elements[i]; }
    const device T& operator[](size_t i) const device { return elements[i]; }
    thread T& operator[](size_t i) thread { return elements[i]; }
    const thread T& operator[](size_t i) const thread { return elements[i]; }
    threadgroup T& operator[](size_t i) threadgroup { return elements[i]; }
    const threadgroup T& operator[](size_t i) const threadgroup { return elements[i]; }
    T elements[N];
};

struct tint_packed_vec3_f32_struct_member {
  /* 0x0000 */ packed_float3 elements;
};

struct S_tint_packed_vec3 {
  /* 0x0000 */ tint_packed_vec3_f32_struct_member v;
  /* 0x000c */ tint_array<int8_t, 4> tint_pad;
};

struct S {
  float3 v;
};

void f(const constant S_tint_packed_vec3* const tint_symbol) {
  float3 v = float3((*(tint_symbol)).v.elements);
  float x = (*(tint_symbol)).v.elements[0];
  float y = (*(tint_symbol)).v.elements[1];
  float z = (*(tint_symbol)).v.elements[2];
  float2 xx = float3((*(tint_symbol)).v.elements).xx;
  float2 xy = float3((*(tint_symbol)).v.elements).xy;
  float2 xz = float3((*(tint_symbol)).v.elements).xz;
  float2 yx = float3((*(tint_symbol)).v.elements).yx;
  float2 yy = float3((*(tint_symbol)).v.elements).yy;
  float2 yz = float3((*(tint_symbol)).v.elements).yz;
  float2 zx = float3((*(tint_symbol)).v.elements).zx;
  float2 zy = float3((*(tint_symbol)).v.elements).zy;
  float2 zz = float3((*(tint_symbol)).v.elements).zz;
  float3 xxx = float3((*(tint_symbol)).v.elements).xxx;
  float3 xxy = float3((*(tint_symbol)).v.elements).xxy;
  float3 xxz = float3((*(tint_symbol)).v.elements).xxz;
  float3 xyx = float3((*(tint_symbol)).v.elements).xyx;
  float3 xyy = float3((*(tint_symbol)).v.elements).xyy;
  float3 xyz = float3((*(tint_symbol)).v.elements).xyz;
  float3 xzx = float3((*(tint_symbol)).v.elements).xzx;
  float3 xzy = float3((*(tint_symbol)).v.elements).xzy;
  float3 xzz = float3((*(tint_symbol)).v.elements).xzz;
  float3 yxx = float3((*(tint_symbol)).v.elements).yxx;
  float3 yxy = float3((*(tint_symbol)).v.elements).yxy;
  float3 yxz = float3((*(tint_symbol)).v.elements).yxz;
  float3 yyx = float3((*(tint_symbol)).v.elements).yyx;
  float3 yyy = float3((*(tint_symbol)).v.elements).yyy;
  float3 yyz = float3((*(tint_symbol)).v.elements).yyz;
  float3 yzx = float3((*(tint_symbol)).v.elements).yzx;
  float3 yzy = float3((*(tint_symbol)).v.elements).yzy;
  float3 yzz = float3((*(tint_symbol)).v.elements).yzz;
  float3 zxx = float3((*(tint_symbol)).v.elements).zxx;
  float3 zxy = float3((*(tint_symbol)).v.elements).zxy;
  float3 zxz = float3((*(tint_symbol)).v.elements).zxz;
  float3 zyx = float3((*(tint_symbol)).v.elements).zyx;
  float3 zyy = float3((*(tint_symbol)).v.elements).zyy;
  float3 zyz = float3((*(tint_symbol)).v.elements).zyz;
  float3 zzx = float3((*(tint_symbol)).v.elements).zzx;
  float3 zzy = float3((*(tint_symbol)).v.elements).zzy;
  float3 zzz = float3((*(tint_symbol)).v.elements).zzz;
  float4 xxxx = float3((*(tint_symbol)).v.elements).xxxx;
  float4 xxxy = float3((*(tint_symbol)).v.elements).xxxy;
  float4 xxxz = float3((*(tint_symbol)).v.elements).xxxz;
  float4 xxyx = float3((*(tint_symbol)).v.elements).xxyx;
  float4 xxyy = float3((*(tint_symbol)).v.elements).xxyy;
  float4 xxyz = float3((*(tint_symbol)).v.elements).xxyz;
  float4 xxzx = float3((*(tint_symbol)).v.elements).xxzx;
  float4 xxzy = float3((*(tint_symbol)).v.elements).xxzy;
  float4 xxzz = float3((*(tint_symbol)).v.elements).xxzz;
  float4 xyxx = float3((*(tint_symbol)).v.elements).xyxx;
  float4 xyxy = float3((*(tint_symbol)).v.elements).xyxy;
  float4 xyxz = float3((*(tint_symbol)).v.elements).xyxz;
  float4 xyyx = float3((*(tint_symbol)).v.elements).xyyx;
  float4 xyyy = float3((*(tint_symbol)).v.elements).xyyy;
  float4 xyyz = float3((*(tint_symbol)).v.elements).xyyz;
  float4 xyzx = float3((*(tint_symbol)).v.elements).xyzx;
  float4 xyzy = float3((*(tint_symbol)).v.elements).xyzy;
  float4 xyzz = float3((*(tint_symbol)).v.elements).xyzz;
  float4 xzxx = float3((*(tint_symbol)).v.elements).xzxx;
  float4 xzxy = float3((*(tint_symbol)).v.elements).xzxy;
  float4 xzxz = float3((*(tint_symbol)).v.elements).xzxz;
  float4 xzyx = float3((*(tint_symbol)).v.elements).xzyx;
  float4 xzyy = float3((*(tint_symbol)).v.elements).xzyy;
  float4 xzyz = float3((*(tint_symbol)).v.elements).xzyz;
  float4 xzzx = float3((*(tint_symbol)).v.elements).xzzx;
  float4 xzzy = float3((*(tint_symbol)).v.elements).xzzy;
  float4 xzzz = float3((*(tint_symbol)).v.elements).xzzz;
  float4 yxxx = float3((*(tint_symbol)).v.elements).yxxx;
  float4 yxxy = float3((*(tint_symbol)).v.elements).yxxy;
  float4 yxxz = float3((*(tint_symbol)).v.elements).yxxz;
  float4 yxyx = float3((*(tint_symbol)).v.elements).yxyx;
  float4 yxyy = float3((*(tint_symbol)).v.elements).yxyy;
  float4 yxyz = float3((*(tint_symbol)).v.elements).yxyz;
  float4 yxzx = float3((*(tint_symbol)).v.elements).yxzx;
  float4 yxzy = float3((*(tint_symbol)).v.elements).yxzy;
  float4 yxzz = float3((*(tint_symbol)).v.elements).yxzz;
  float4 yyxx = float3((*(tint_symbol)).v.elements).yyxx;
  float4 yyxy = float3((*(tint_symbol)).v.elements).yyxy;
  float4 yyxz = float3((*(tint_symbol)).v.elements).yyxz;
  float4 yyyx = float3((*(tint_symbol)).v.elements).yyyx;
  float4 yyyy = float3((*(tint_symbol)).v.elements).yyyy;
  float4 yyyz = float3((*(tint_symbol)).v.elements).yyyz;
  float4 yyzx = float3((*(tint_symbol)).v.elements).yyzx;
  float4 yyzy = float3((*(tint_symbol)).v.elements).yyzy;
  float4 yyzz = float3((*(tint_symbol)).v.elements).yyzz;
  float4 yzxx = float3((*(tint_symbol)).v.elements).yzxx;
  float4 yzxy = float3((*(tint_symbol)).v.elements).yzxy;
  float4 yzxz = float3((*(tint_symbol)).v.elements).yzxz;
  float4 yzyx = float3((*(tint_symbol)).v.elements).yzyx;
  float4 yzyy = float3((*(tint_symbol)).v.elements).yzyy;
  float4 yzyz = float3((*(tint_symbol)).v.elements).yzyz;
  float4 yzzx = float3((*(tint_symbol)).v.elements).yzzx;
  float4 yzzy = float3((*(tint_symbol)).v.elements).yzzy;
  float4 yzzz = float3((*(tint_symbol)).v.elements).yzzz;
  float4 zxxx = float3((*(tint_symbol)).v.elements).zxxx;
  float4 zxxy = float3((*(tint_symbol)).v.elements).zxxy;
  float4 zxxz = float3((*(tint_symbol)).v.elements).zxxz;
  float4 zxyx = float3((*(tint_symbol)).v.elements).zxyx;
  float4 zxyy = float3((*(tint_symbol)).v.elements).zxyy;
  float4 zxyz = float3((*(tint_symbol)).v.elements).zxyz;
  float4 zxzx = float3((*(tint_symbol)).v.elements).zxzx;
  float4 zxzy = float3((*(tint_symbol)).v.elements).zxzy;
  float4 zxzz = float3((*(tint_symbol)).v.elements).zxzz;
  float4 zyxx = float3((*(tint_symbol)).v.elements).zyxx;
  float4 zyxy = float3((*(tint_symbol)).v.elements).zyxy;
  float4 zyxz = float3((*(tint_symbol)).v.elements).zyxz;
  float4 zyyx = float3((*(tint_symbol)).v.elements).zyyx;
  float4 zyyy = float3((*(tint_symbol)).v.elements).zyyy;
  float4 zyyz = float3((*(tint_symbol)).v.elements).zyyz;
  float4 zyzx = float3((*(tint_symbol)).v.elements).zyzx;
  float4 zyzy = float3((*(tint_symbol)).v.elements).zyzy;
  float4 zyzz = float3((*(tint_symbol)).v.elements).zyzz;
  float4 zzxx = float3((*(tint_symbol)).v.elements).zzxx;
  float4 zzxy = float3((*(tint_symbol)).v.elements).zzxy;
  float4 zzxz = float3((*(tint_symbol)).v.elements).zzxz;
  float4 zzyx = float3((*(tint_symbol)).v.elements).zzyx;
  float4 zzyy = float3((*(tint_symbol)).v.elements).zzyy;
  float4 zzyz = float3((*(tint_symbol)).v.elements).zzyz;
  float4 zzzx = float3((*(tint_symbol)).v.elements).zzzx;
  float4 zzzy = float3((*(tint_symbol)).v.elements).zzzy;
  float4 zzzz = float3((*(tint_symbol)).v.elements).zzzz;
}

