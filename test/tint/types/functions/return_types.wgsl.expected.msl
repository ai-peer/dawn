#include <metal_stdlib>

using namespace metal;

template<typename T, size_t N>
struct tint_array {
    const constant T& operator[](size_t i) const constant { return elements[i]; }
    device T& operator[](size_t i) device { return elements[i]; }
    const device T& operator[](size_t i) const device { return elements[i]; }
    thread T& operator[](size_t i) thread { return elements[i]; }
    const thread T& operator[](size_t i) const thread { return elements[i]; }
    threadgroup T& operator[](size_t i) threadgroup { return elements[i]; }
    const threadgroup T& operator[](size_t i) const threadgroup { return elements[i]; }
    T elements[N];
};

struct S {
  float a;
};

bool ret_bool() {
  volatile bool tint_loop_preserving_true = true;
  return false;
}

int ret_i32() {
  volatile bool tint_loop_preserving_true_1 = true;
  return 0;
}

uint ret_u32() {
  volatile bool tint_loop_preserving_true_2 = true;
  return 0u;
}

float ret_f32() {
  volatile bool tint_loop_preserving_true_3 = true;
  return 0.0f;
}

int2 ret_v2i32() {
  volatile bool tint_loop_preserving_true_4 = true;
  return int2(0);
}

uint3 ret_v3u32() {
  volatile bool tint_loop_preserving_true_5 = true;
  return uint3(0u);
}

float4 ret_v4f32() {
  volatile bool tint_loop_preserving_true_6 = true;
  return float4(0.0f);
}

float2x3 ret_m2x3() {
  volatile bool tint_loop_preserving_true_7 = true;
  return float2x3(float3(0.0f), float3(0.0f));
}

tint_array<float, 4> ret_arr() {
  volatile bool tint_loop_preserving_true_8 = true;
  tint_array<float, 4> const tint_symbol_1 = tint_array<float, 4>{};
  return tint_symbol_1;
}

S ret_struct() {
  volatile bool tint_loop_preserving_true_9 = true;
  S const tint_symbol_2 = S{};
  return tint_symbol_2;
}

kernel void tint_symbol() {
  volatile bool tint_loop_preserving_true_10 = true;
  return;
}

