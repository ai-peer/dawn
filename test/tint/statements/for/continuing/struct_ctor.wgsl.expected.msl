#include <metal_stdlib>

using namespace metal;
struct S {
  int i;
};

void f() {
  volatile bool tint_loop_preserving_true = true;
  for(int i = 0; (false) && tint_loop_preserving_true; i = as_type<int>((as_type<uint>(i) + as_type<uint>(1)))) {
  }
}

