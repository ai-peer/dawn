#include <metal_stdlib>

using namespace metal;
void tint_symbol(device uint* const tint_symbol_1) {
  volatile bool tint_loop_preserving_true = true;
  for(*(tint_symbol_1) = (*(tint_symbol_1) - 1u); ((*(tint_symbol_1) < 10u)) && tint_loop_preserving_true; ) {
  }
}

