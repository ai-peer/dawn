SKIP: FAILED

#include <metal_stdlib>
using namespace metal;
struct str {
  float4 i;
};

threadgroup str S;
void func(threadgroup float4* pointer) {
  pointer = float4(0.0f);
}
kernel void tint_symbol() {
  func(S.i);
}
program_source:7:17: error: program scope variable must reside in constant address space
threadgroup str S;
                ^
program_source:9:13: error: assigning to 'threadgroup float4 *' from incompatible type 'float4' (vector of 4 'float' values)
  pointer = float4(0.0f);
            ^~~~~~~~~~~~

