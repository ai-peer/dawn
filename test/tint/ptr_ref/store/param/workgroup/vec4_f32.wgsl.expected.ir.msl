SKIP: FAILED

#include <metal_stdlib>
using namespace metal;

threadgroup float4 S;
void func(threadgroup float4* pointer) {
  pointer = float4(0.0f);
}
kernel void tint_symbol() {
  func(S);
}
program_source:4:20: error: program scope variable must reside in constant address space
threadgroup float4 S;
                   ^
program_source:6:13: error: assigning to 'threadgroup float4 *' from incompatible type 'float4' (vector of 4 'float' values)
  pointer = float4(0.0f);
            ^~~~~~~~~~~~

