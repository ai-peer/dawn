SKIP: FAILED

#include <metal_stdlib>
using namespace metal;

thread float2x2 P = float2x2(0.0f);
void func(thread float2* pointer) {
  pointer = float2(0.0f);
}
kernel void tint_symbol() {
  func(P[1]);
}
program_source:4:17: error: program scope variable must reside in constant address space
thread float2x2 P = float2x2(0.0f);
                ^
program_source:6:13: error: assigning to 'float2 *' from incompatible type 'float2' (vector of 2 'float' values)
  pointer = float2(0.0f);
            ^~~~~~~~~~~~

