SKIP: FAILED

#include <metal_stdlib>
using namespace metal;
struct str {
  float4 i;
};

thread str P = {};
void func(thread float4* pointer) {
  pointer = float4(0.0f);
}
kernel void tint_symbol() {
  func(P.i);
}
program_source:7:12: error: program scope variable must reside in constant address space
thread str P = {};
           ^
program_source:9:13: error: assigning to 'float4 *' from incompatible type 'float4' (vector of 4 'float' values)
  pointer = float4(0.0f);
            ^~~~~~~~~~~~

