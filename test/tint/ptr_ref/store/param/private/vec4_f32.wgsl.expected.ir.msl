SKIP: FAILED

#include <metal_stdlib>
using namespace metal;

thread float4 P = 0.0f;
void func(thread float4* pointer) {
  pointer = float4(0.0f);
}
kernel void tint_symbol() {
  func(P);
}
program_source:4:15: error: program scope variable must reside in constant address space
thread float4 P = 0.0f;
              ^
program_source:6:13: error: assigning to 'float4 *' from incompatible type 'float4' (vector of 4 'float' values)
  pointer = float4(0.0f);
            ^~~~~~~~~~~~

